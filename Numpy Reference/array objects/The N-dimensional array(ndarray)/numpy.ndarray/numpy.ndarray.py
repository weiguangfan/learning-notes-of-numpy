"""
class numpy.ndarray(
                    shape,
                    dtype=float,
                    buffer=None,
                    offset=0,
                    strides=None,
                    order=None)
一个数组对象表示一个多维的、由固定大小的项目组成的同质数组。
一个相关的数据类型对象描述了数组中每个元素的格式（它的字节顺序，它在内存中占据多少个字节，它是一个整数，一个浮点数，还是其他什么，等等）。

数组应该使用数组、零或空来构建（参考下面的See Also部分）。
这里给出的参数是指一个用于实例化数组的低级方法（ndarray(..)）。

更多信息，请参考numpy模块并研究数组的方法和属性。

"""